[project]
name = "hugin-browser-mcp"
version = "0.1.0"
description = "MCP browser automation tools for Hugin"
requires-python = ">=3.11"
dependencies = [
  "mcp",
  "playwright",
  "pillow",
  "pytesseract",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
